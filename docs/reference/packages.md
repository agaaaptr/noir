# Package Inventory

> Auto-generated from `packages/*/package.json`.

**Source version:** `1.7.4`

| Package | Version | Description | Binary |
|---|---|---|---|
| `@noir-ai/adapters` | 1.7.4 | Noir adapters — the HostAdapter abstraction and the Claude Code adapter (the v1 host). | — |
| `@noir-ai/cli` | 1.7.4 | Noir CLI — the `noir` command tree (commander + @clack/prompts), the shell entry point to the Noir AI toolkit. | `noir` |
| `@noir-ai/context` | 1.7.4 | Noir context — SHA-256 incremental indexer and hybrid retrieval (BM25 + vector kNN + Reciprocal Rank Fusion). | — |
| `@noir-ai/core` | 1.7.4 | Noir core — shared types, the config schema (NoirConfigSchema), the .noir/ layout, and markers. | — |
| `@noir-ai/create` | 1.7.4 | Noir scaffold engine — the declarative three-mode writer, manifest, templates, stack-detect, and migrations that power `noir init`/`create`/`sync`. | — |
| `@noir-ai/daemon` | 1.7.4 | Noir daemon — the runtime authority: owns the store write handle and exposes the single Noir MCP server (stdio + Streamable HTTP). | — |
| `@noir-ai/memory` | 1.7.4 | Noir memory — cross-session observations over the store, with hybrid recall and provider-gated consolidation. | — |
| `@noir-ai/model` | 1.7.4 | Noir model — optional bounded single-shot LLM layer (anthropic / openai / openai-compatible); agent loops are impossible by design. | — |
| `@noir-ai/skills` | 1.7.4 | Noir skills — the builtin noir-* skill pack, the integrations pack (ClickUp first), and a copy-and-validate compiler. | — |
| `@noir-ai/store` | 1.7.4 | Noir store — embedded storage: better-sqlite3 + FTS5 (BM25, window snippets) + sqlite-vec (384-dim kNN). | — |
| `@noir-ai/workflow` | 1.7.4 | Noir workflow — the spec-driven lifecycle engine (intake → document) with observable, escapable gates. | — |
