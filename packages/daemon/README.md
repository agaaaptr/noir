# @noir-ai/daemon

The runtime authority: owns the store write handle, resolves the embedder once, and exposes the single Noir MCP server (stdio + Streamable HTTP). Part of the Noir toolkit. See the [root README](../../README.md).
