# @noir-ai/model

Optional bounded model layer — single-shot completion, provider-explicit, null-degrades without a key (Anthropic / OpenAI / OpenAI-compatible via fetch). No agent loop, by design. Part of the Noir toolkit. See the [root README](../../README.md).
