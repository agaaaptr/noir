# @noir-ai/adapters

The host abstraction (`HostAdapter`); v1 ships the Claude Code adapter (`.mcp.json` + `CLAUDE.md` @import + `.claude/skills/`). Part of the Noir toolkit. See the [root README](../../README.md).
