# Local SDD / superpowers scratch (session planning state, not source)
.superpowers/

# OS / editor
.DS_Store

# Build / dependencies
node_modules/
dist/
*.tsbuildinfo
coverage/

# Release pack output (pnpm pack --pack-destination)
artifacts/
