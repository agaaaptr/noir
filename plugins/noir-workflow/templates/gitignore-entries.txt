# Entries /init appends to a project's .gitignore (noir-workflow standard layout).
# workflow/            — in-flight task state (owned by the workflow; ephemeral/personal)
# .session/            — session scratch (promote-or-delete each session)
# .superpowers/sdd/    — Superpowers SDD scratch (cleaned each session)
# .notes/ is project-dependent — /init includes it only if the project uses it.
workflow/
.session/
.superpowers/
